package org.dreeam.leaf.config;

/**
 * Marker for a world-scoped Leaf configuration module.
 *
 * <p>Annotated fields must be mutable instance fields. Implementations must be exposed as typed
 * fields on {@link LeafWorldConfig}.</p>
 */
public interface WorldConfigModule {
}
