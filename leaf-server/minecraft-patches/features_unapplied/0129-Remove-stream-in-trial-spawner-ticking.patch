From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Dreeam <61569423+Dreeam-qwq@users.noreply.github.com>
Date: Sat, 5 Oct 2024 15:39:15 -0400
Subject: [PATCH] Remove stream in trial spawner ticking


diff --git a/net/minecraft/world/level/block/entity/trialspawner/TrialSpawnerState.java b/net/minecraft/world/level/block/entity/trialspawner/TrialSpawnerState.java
index 545724bc76a88cdbf5e2cee4e78296c085c5ffdb..efe49e2f878bebcffd2bf4fad229a95c5f9eb4d2 100644
--- a/net/minecraft/world/level/block/entity/trialspawner/TrialSpawnerState.java
+++ b/net/minecraft/world/level/block/entity/trialspawner/TrialSpawnerState.java
@@ -176,17 +176,21 @@ public enum TrialSpawnerState implements StringRepresentable {
     private static Optional<Vec3> calculatePositionToSpawnSpawner(
         final ServerLevel level, final BlockPos trialSpawnerPos, final TrialSpawner trialSpawner, final TrialSpawnerStateData data
     ) {
-        List<Player> nearbyPlayers = data.detectedPlayers
-            .stream()
-            .map(level::getPlayerByUUID)
-            .filter(Objects::nonNull)
-            .filter(
-                player -> !player.isCreative()
-                    && !player.isSpectator()
-                    && player.isAlive()
-                    && player.distanceToSqr(Vec3.atCenterOf(trialSpawnerPos)) <= Mth.square(trialSpawner.getRequiredPlayerRange())
-            )
-            .toList();
+        // Leaf start - Remove stream in trial spawner ticking
+        List<Player> nearbyPlayers = new java.util.ArrayList<>();
+
+        for (UUID uuid : data.detectedPlayers) {
+            Player player = level.getPlayerByUUID(uuid);
+
+            if (player != null
+                && !player.isCreative()
+                && !player.isSpectator()
+                && player.isAlive()
+                && player.distanceToSqr(Vec3.atCenterOf(trialSpawnerPos)) <= Mth.square(trialSpawner.getRequiredPlayerRange())) {
+                nearbyPlayers.add(player);
+            }
+        }
+        // Leaf end - Remove stream in trial spawner ticking
         if (nearbyPlayers.isEmpty()) {
             return Optional.empty();
         }
@@ -209,17 +213,30 @@ public enum TrialSpawnerState implements StringRepresentable {
     private static @Nullable Entity selectEntityToSpawnItemAbove(
         final List<Player> nearbyPlayers, final Set<UUID> mobIds, final TrialSpawner trialSpawner, final BlockPos spawnerPos, final ServerLevel level
     ) {
-        Stream<Entity> nearbyMobs = mobIds.stream()
-            .map(level::getEntity)
-            .filter(Objects::nonNull)
-            .filter(target -> target.isAlive() && target.distanceToSqr(Vec3.atCenterOf(spawnerPos)) <= Mth.square(trialSpawner.getRequiredPlayerRange()));
+        // Leaf start - Remove stream in trial spawner ticking
         RandomSource random = level.getRandom();
-        List<? extends Entity> eligibleEntities = random.nextBoolean() ? nearbyMobs.toList() : nearbyPlayers;
-        if (eligibleEntities.isEmpty()) {
-            return null;
+        if (random.nextBoolean()) {
+            List<Entity> nearbyMobs = new java.util.ArrayList<>();
+            for (UUID uuid : mobIds) {
+                Entity target = level.getEntity(uuid);
+                if (target != null && target.isAlive() && target.distanceToSqr(Vec3.atCenterOf(spawnerPos)) <= Mth.square(trialSpawner.getRequiredPlayerRange())) {
+                    nearbyMobs.add(target);
+                }
+            }
+
+            if (nearbyMobs.isEmpty()) {
+                return null;
+            } else {
+                return nearbyMobs.size() == 1 ? nearbyMobs.getFirst() : Util.getRandom(nearbyMobs, random);
+            }
         } else {
-            return eligibleEntities.size() == 1 ? eligibleEntities.getFirst() : Util.getRandom(eligibleEntities, random);
+            if (nearbyPlayers.isEmpty()) {
+                return null;
+            } else {
+                return nearbyPlayers.size() == 1 ? nearbyPlayers.getFirst() : Util.getRandom(nearbyPlayers, random);
+            }
         }
+        // Leaf end - Remove stream in trial spawner ticking
     }
 
     private boolean timeToSpawnItemSpawner(final ServerLevel serverLevel, final TrialSpawnerStateData data) {
