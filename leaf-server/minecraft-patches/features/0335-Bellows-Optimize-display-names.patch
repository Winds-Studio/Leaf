From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Onako2 <79749977+Onako2@users.noreply.github.com>
Date: Thu, 10 Sep 2026 18:22:33 +0200
Subject: [PATCH] Bellows: Optimize display names


diff --git a/net/minecraft/commands/CommandSourceStack.java b/net/minecraft/commands/CommandSourceStack.java
index 3783382a712930b49fa5211342984d47faedd0e5..e75e8f84fbdb30706a9c48298707496d28898768 100644
--- a/net/minecraft/commands/CommandSourceStack.java
+++ b/net/minecraft/commands/CommandSourceStack.java
@@ -66,6 +66,10 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
     private final CommandSigningContext signingContext;
     private final TaskChainer chatMessageChainer;
     public boolean bypassSelectorPermissions = false; // Paper - add bypass for selector permissions
+    // Leaf start - Bellows - Optimize display names
+    private @Nullable Supplier<String> textNameSupplier = null;
+    private @Nullable Supplier<Component> displayNameSupplier = null;
+    // Leaf end
 
     public CommandSourceStack(
         final CommandSource source,
@@ -129,7 +133,8 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
     }
 
     public CommandSourceStack withSource(final CommandSource source) {
-        return this.source == source
+        return copySuppliers( // Leaf - Bellows - Optimize display names
+            this.source == source
             ? this
             : new CommandSourceStack(
                 source,
@@ -146,20 +151,22 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
                 this.anchor,
                 this.signingContext,
                 this.chatMessageChainer
-            );
+            )
+        );
     }
 
     public CommandSourceStack withEntity(final Entity entity) {
         return this.entity == entity
             ? this
-            : new CommandSourceStack(
+            : setSuppliers( // Leaf - Bellows - Optimize display names
+            new CommandSourceStack(
                 this.source,
                 this.worldPosition,
                 this.rotation,
                 this.level,
                 this.permissions,
-                entity.getPlainTextName(),
-                entity.getDisplayName(),
+                null, // Leaf - Bellows - Optimize display names
+                null, // Leaf - Bellows - Optimize display names
                 this.server,
                 entity,
                 this.silent,
@@ -167,11 +174,27 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
                 this.anchor,
                 this.signingContext,
                 this.chatMessageChainer
-            );
+            ), entity
+        );
+    }
+
+    // Leaf start - Bellows - Optimize display names
+    private CommandSourceStack setSuppliers(CommandSourceStack stack, Entity entity) {
+        stack.textNameSupplier = entity.getPlainTextNameSupplier();
+        stack.displayNameSupplier = entity.getDisplayNameSupplier();
+        return stack;
     }
 
+    private CommandSourceStack copySuppliers(CommandSourceStack stack) {
+        stack.textNameSupplier = this.textNameSupplier;
+        stack.displayNameSupplier = this.displayNameSupplier;
+        return stack;
+    }
+    // Leaf end - Bellows - Optimize display names
+
     public CommandSourceStack withPosition(final Vec3 pos) {
-        return this.worldPosition.equals(pos)
+        return copySuppliers( // Leaf - Bellows - Optimize display names
+            this.worldPosition.equals(pos)
             ? this
             : new CommandSourceStack(
                 this.source,
@@ -188,13 +211,15 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
                 this.anchor,
                 this.signingContext,
                 this.chatMessageChainer
-            );
+            )
+        );
     }
 
     // Paper start - Expose 'with' functions from the CommandSourceStack
     @Override
     public CommandSourceStack withLocation(org.bukkit.Location location) {
-        return this.getLocation().equals(location)
+        return copySuppliers( // Leaf - Bellows - Optimize display names
+            this.getLocation().equals(location)
             ? this
             : new CommandSourceStack(
             this.source,
@@ -211,12 +236,14 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
             this.anchor,
             this.signingContext,
             this.chatMessageChainer
+            )
         );
     }
     // Paper end - Expose 'with' functions from the CommandSourceStack
 
     public CommandSourceStack withRotation(final Vec2 rotation) {
-        return this.rotation.equals(rotation)
+        return copySuppliers( // Leaf - Bellows - Optimize display names
+            this.rotation.equals(rotation)
             ? this
             : new CommandSourceStack(
                 this.source,
@@ -233,12 +260,14 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
                 this.anchor,
                 this.signingContext,
                 this.chatMessageChainer
-            );
+            )
+        );
     }
 
     @Override
     public CommandSourceStack withCallback(final CommandResultCallback resultCallback) {
-        return Objects.equals(this.resultCallback, resultCallback)
+        return copySuppliers( // Leaf - Bellows - Optimize display names
+            Objects.equals(this.resultCallback, resultCallback)
             ? this
             : new CommandSourceStack(
                 this.source,
@@ -255,7 +284,8 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
                 this.anchor,
                 this.signingContext,
                 this.chatMessageChainer
-            );
+            )
+        );
     }
 
     public CommandSourceStack withCallback(final CommandResultCallback newCallback, final BinaryOperator<CommandResultCallback> combiner) {
@@ -264,7 +294,8 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
     }
 
     public CommandSourceStack withSuppressedOutput() {
-        return !this.silent && !this.source.alwaysAccepts()
+        return copySuppliers( // Leaf - Bellows - Optimize display names
+            !this.silent && !this.source.alwaysAccepts()
             ? new CommandSourceStack(
                 this.source,
                 this.worldPosition,
@@ -281,11 +312,13 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
                 this.signingContext,
                 this.chatMessageChainer
             )
-            : this;
+            : this
+        );
     }
 
     public CommandSourceStack withPermission(final PermissionSet permissions) {
-        return permissions == this.permissions
+        return copySuppliers( // Leaf - Bellows - Optimize display names
+            permissions == this.permissions
             ? this
             : new CommandSourceStack(
                 this.source,
@@ -302,7 +335,8 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
                 this.anchor,
                 this.signingContext,
                 this.chatMessageChainer
-            );
+            )
+        );
     }
 
     public CommandSourceStack withMaximumPermission(final PermissionSet newPermissions) {
@@ -310,7 +344,8 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
     }
 
     public CommandSourceStack withAnchor(final EntityAnchorArgument.Anchor anchor) {
-        return anchor == this.anchor
+        return copySuppliers( // Leaf - Bellows - Optimize display names
+            anchor == this.anchor
             ? this
             : new CommandSourceStack(
                 this.source,
@@ -327,7 +362,8 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
                 anchor,
                 this.signingContext,
                 this.chatMessageChainer
-            );
+            )
+        );
     }
 
     public CommandSourceStack withLevel(final ServerLevel level) {
@@ -337,7 +373,7 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
 
         double scale = DimensionType.getTeleportationScale(this.level.dimensionType(), level.dimensionType());
         Vec3 pos = new Vec3(this.worldPosition.x * scale, this.worldPosition.y, this.worldPosition.z * scale);
-        return new CommandSourceStack(
+        return copySuppliers(new CommandSourceStack( // Leaf - Bellows - Optimize display names
             this.source,
             pos,
             this.rotation,
@@ -352,7 +388,7 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
             this.anchor,
             this.signingContext,
             this.chatMessageChainer
-        );
+        ));
     }
 
     public CommandSourceStack facing(final Entity entity, final EntityAnchorArgument.Anchor anchor) {
@@ -371,7 +407,8 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
     }
 
     public CommandSourceStack withSigningContext(final CommandSigningContext signingContext, final TaskChainer chatMessageChainer) {
-        return signingContext == this.signingContext && chatMessageChainer == this.chatMessageChainer
+        return copySuppliers( // Leaf - Bellows - Optimize display names
+            signingContext == this.signingContext && chatMessageChainer == this.chatMessageChainer
             ? this
             : new CommandSourceStack(
                 this.source,
@@ -388,14 +425,17 @@ public class CommandSourceStack implements SharedSuggestionProvider, ExecutionCo
                 this.anchor,
                 signingContext,
                 chatMessageChainer
-            );
+            )
+        );
     }
 
     public Component getDisplayName() {
+        if (displayName == null) displayName = displayNameSupplier.get(); // Leaf - Bellows - Optimize display names
         return this.displayName;
     }
 
     public String getTextName() {
+        if (textName == null) textName = textNameSupplier.get(); // Leaf - Bellows - Optimize display names
         return this.textName;
     }
 
diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index 0ac10f8283ebf0f897a9df1c6ec0e4bd7a2f732b..e7ccc58724c07ff0ea51ceec1c7d1e251d8bfc6c 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -4219,6 +4219,12 @@ public abstract class Entity
         return customName != null ? removeAction(customName) : this.getTypeName();
     }
 
+    // Leaf start - Bellows - Optimize display names
+    public java.util.function.Supplier<String> getPlainTextNameSupplier() {
+        return () -> this.getName().getString();
+    }
+    // Leaf end - Bellows - Optimize display names
+
     protected Component getTypeName() {
         return this.type.getDescription();
     }
@@ -4642,6 +4648,12 @@ public abstract class Entity
             .withStyle(s -> s.withHoverEvent(this.createHoverEvent()).withInsertion(this.getStringUUID()));
     }
 
+    // Leaf start - Bellows - Optimize display names
+    public java.util.function.Supplier<net.minecraft.network.chat.Component> getDisplayNameSupplier() {
+        return this::getDisplayName;
+    }
+    // Leaf end - Bellows - Optimize display names
+
     public void setCustomName(final @Nullable Component name) {
         this.entityData.set(DATA_CUSTOM_NAME, Optional.ofNullable(name));
     }
