diff --git a/src/main/java/net/minecraft/world/entity/player/Player.java b/src/main/java/net/minecraft/world/entity/player/Player.java
index 0000000000..1111111111
--- a/src/main/java/net/minecraft/world/entity/player/Player.java
+++ b/src/main/java/net/minecraft/world/entity/player/Player.java
@@ -1,3 +1,8 @@
+    @Override
+    public boolean isPushable() {
+        if (org.dreeam.leaf.config.LeafConfig.pvpOptimizeEnabled) return false; // BOXPVP: Tắt va chạm người chơi
+        return true;
+    }

diff --git b/src/main/java/net/minecraft/world/entity/projectile/FireworkRocketEntity.java b/src/main/java/net/minecraft/world/entity/projectile/FireworkRocketEntity.java
index 0000000000..2222222222
--- a/src/main/java/net/minecraft/world/entity/projectile/FireworkRocketEntity.java
+++ b/src/main/java/net/minecraft/world/entity/projectile/FireworkRocketEntity.java
@@ -1,3 +1,20 @@
+    @Override
+    public void tick() {
+        // ĐẶC TRỊ PHÁO HOA ELYTRA: Ngắt khói đuôi pháo hoa
+        if (org.dreeam.leaf.config.LeafConfig.pvpOptimizeEnabled && this.isAttachedToEntity()) {
+            this.hasImpacted = false;
+            if (!this.level().isClientSide && this.tickCount > this.lifetime) {
+                this.explode();
+            }
+            return; 
+        }
+        super.tick();
+    }
+
+    private void explode() {
+        if (org.dreeam.leaf.config.LeafConfig.pvpOptimizeEnabled) {
+            this.discard(); // Triệt tiêu hoàn toàn hạt vòng lửa nổ
+            return;
+        }
+    }

diff --git b/src/main/java/net/minecraft/world/level/Explosion.java b/src/main/java/net/minecraft/world/level/Explosion.java
index 0000000000..3333333333
--- a/src/main/java/net/minecraft/world/level/Explosion.java
+++ b/src/main/java/net/minecraft/world/level/Explosion.java
@@ -1,3 +1,11 @@
+    public void finalizeExplosion(boolean spawnParticles) {
+        // TỐI ƯU CRYSTAL & ANCHOR: Triệt tiêu hạt khói bụi nổ
+        if (org.dreeam.leaf.config.LeafConfig.pvpOptimizeEnabled) {
+            if (this.level != null) {
+                this.level.playSound(null, this.x, this.y, this.z, net.minecraft.sound.SoundEvents.GENERIC_EXPLODE, net.minecraft.sound.SoundSource.BLOCKS, 4.0F, (1.0F + (this.level.random.nextFloat() - this.level.random.nextFloat()) * 0.2F) * 0.7F);
+            }
+            this.clearToBlow();
+            return;
+        }
+    }

diff --git b/src/main/java/net/minecraft/world/level/block/RespawnAnchorBlock.java b/src/main/java/net/minecraft/world/level/block/RespawnAnchorBlock.java
index 0000000000..4444444444
--- a/src/main/java/net/minecraft/world/level/block/RespawnAnchorBlock.java
+++ b/d/backend/src/main/java/net/minecraft/world/level/block/RespawnAnchorBlock.java
@@ -1,3 +1,7 @@
+    @Override
+    public void animateTick(net.minecraft.world.level.block.state.BlockState state, net.minecraft.world.level.Level world, net.minecraft.core.BlockPos pos, net.minecraft.util.RandomSource random) {
+        if (org.dreeam.leaf.config.LeafConfig.pvpOptimizeEnabled) return; // Ẩn bụi lấp lánh xung quanh Anchor
+    }

diff --git b/src/main/java/net/minecraft/world/level/block/FallingBlock.java b/src/main/java/net/minecraft/world/level/block/FallingBlock.java
index 0000000000..4545454545
--- a/src/main/java/net/minecraft/world/level/block/FallingBlock.java
+++ b/src/main/java/net/minecraft/world/level/block/FallingBlock.java
@@ -1,3 +1,10 @@
+    @Override
+    public void tick(net.minecraft.world.level.block.state.BlockState state, net.minecraft.server.level.ServerLevel world, net.minecraft.core.BlockPos pos, net.minecraft.util.RandomSource random) {
+        // CHẶN BLOCK RƠI: Cát/Sỏi lơ lửng, không rơi thành Entity gây lag chuỗi
+        if (org.dreeam.leaf.config.LeafConfig.disableFallingBlocks) {
+            return;
+        }
+    }

diff --git b/src/main/java/net/minecraft/world/effect/MobEffectInstance.java b/src/main/java/net/minecraft/world/effect/MobEffectInstance.java
index 0000000000..5555555555
--- a/src/main/java/net/minecraft/world/effect/MobEffectInstance.java
+++ b/d/backend/src/main/java/net/minecraft/world/effect/MobEffectInstance.java
@@ -1,3 +1,7 @@
+    public boolean isVisible() {
+        if (org.dreeam.leaf.config.LeafConfig.pvpOptimizeEnabled) return false; // Ẩn bong bóng hiệu ứng thuốc
+        return true;
+    }

diff --git b/src/main/java/net/minecraft/server/level/ServerPlayerGameMode.java b/src/main/java/net/minecraft/server/level/ServerPlayerGameMode.java
index 0000000000..6666666666
--- a/src/main/java/net/minecraft/server/level/ServerPlayerGameMode.java
+++ b/src/main/java/net/minecraft/server/level/ServerPlayerGameMode.java
@@ -1,3 +1,10 @@
+    public void destroyBlock(net.minecraft.core.BlockPos pos) {
+        // BOXPVP SPECIAL: Triệt tiêu hạt hiệu ứng vỡ vụn khi đào mỏ quặng
+        if (org.dreeam.leaf.config.LeafConfig.pvpOptimizeEnabled) {
+            this.level.destroyBlock(pos, true, this.player);
+            return;
+        }
+    }

diff --git b/src/main/java/net/minecraft/server/players/PlayerList.java b/src/main/java/net/minecraft/server/players/PlayerList.java
index 0000000000..7777777777
--- a/src/main/java/net/minecraft/server/players/PlayerList.java
+++ b/src/main/java/net/minecraft/server/players/PlayerList.java
@@ -1,3 +1,14 @@
+    public void broadcast(net.minecraft.world.entity.player.Player except, double x, double y, double z, double distance, net.minecraft.resources.ResourceKey<net.minecraft.world.level.Level> worldKey, net.minecraft.network.protocol.Packet<?> packet) {
+        // BÓP TẦM TRUYỀN PACKET: Giới hạn bán kính gửi hạt/âm thanh về Client còn 32 block
+        double maxDist = (org.dreeam.leaf.config.LeafConfig.pvpOptimizeEnabled && (packet instanceof net.minecraft.network.protocol.game.ClientboundLevelParticlesPacket || packet instanceof net.minecraft.network.protocol.game.ClientboundSoundPacket)) ? 32.0D : distance;
+        for (ServerPlayer serverPlayer : this.players) {
+            if (serverPlayer != except && serverPlayer.level().getDimensionKey() == worldKey) {
+                double d4 = x - serverPlayer.getX();
+                double d5 = y - serverPlayer.getY();
+                double d6 = z - serverPlayer.getZ();
+                if (d4 * d4 + d5 * d5 + d6 * d6 < maxDist * maxDist) {
+                    serverPlayer.connection.send(packet);
+                }
+            }
+        }
+    }