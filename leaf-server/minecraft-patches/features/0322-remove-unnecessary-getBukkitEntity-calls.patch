From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: OverwriteMC <artyom030803@gmail.com>
Date: Tue, 28 Apr 2026 12:59:22 +0300
Subject: [PATCH] remove unnecessary getBukkitEntity calls


diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index de82681bc4c647479e4c32041a10f5c3af92c86d..13240c7e7dcfeb18a06cf02198c5c15172ec2699 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -3204,13 +3204,13 @@ public class ServerLevel extends Level implements ServerEntityGetter, WorldGenLe
             }
             // Spigot end
             // Spigot start
-            if (entity.getBukkitEntity() instanceof org.bukkit.inventory.InventoryHolder && (!(entity instanceof ServerPlayer) || entity.getRemovalReason() != Entity.RemovalReason.KILLED)) { // SPIGOT-6876: closeInventory clears death message
+            if (entity.getBukkitEntity() instanceof org.bukkit.inventory.InventoryHolder inventoryHolder && (!(entity instanceof ServerPlayer) || entity.getRemovalReason() != Entity.RemovalReason.KILLED)) { // SPIGOT-6876: closeInventory clears death message // Leaf only get bukkit entity once
                 // Paper start - Fix merchant inventory not closing on entity removal
                 if (!entity.level().purpurConfig.playerVoidTrading && entity.getBukkitEntity() instanceof org.bukkit.inventory.Merchant merchant && merchant.getTrader() != null) { // Purpur - Allow void trading
                     merchant.getTrader().closeInventory(org.bukkit.event.inventory.InventoryCloseEvent.Reason.UNLOADED);
                 }
                 // Paper end - Fix merchant inventory not closing on entity removal
-                for (org.bukkit.entity.HumanEntity h : Lists.newArrayList(((org.bukkit.inventory.InventoryHolder) entity.getBukkitEntity()).getInventory().getViewers())) {
+                for (org.bukkit.entity.HumanEntity h : Lists.newArrayList(inventoryHolder.getInventory().getViewers())) { // Leaf - only get bukkit entity once
                     h.closeInventory(org.bukkit.event.inventory.InventoryCloseEvent.Reason.UNLOADED); // Paper - Inventory close reason
                 }
             }
diff --git a/net/minecraft/server/players/PlayerList.java b/net/minecraft/server/players/PlayerList.java
index 6c4f131ce8186bed5daf2f6488e2f43eeb60b705..c52f4cc069754f6e79839f198b75ec17357fc450 100644
--- a/net/minecraft/server/players/PlayerList.java
+++ b/net/minecraft/server/players/PlayerList.java
@@ -299,7 +299,8 @@ public abstract class PlayerList {
                 this.server.enforceSecureProfile()
             )
         );
-        player.getBukkitEntity().sendSupportedChannels(); // CraftBukkit
+        org.bukkit.craftbukkit.entity.CraftPlayer bukkitPlayer = player.getBukkitEntity(); // Leaf - only get bukkit entity once
+        bukkitPlayer.sendSupportedChannels(); // CraftBukkit
         serverGamePacketListenerImpl.send(new ClientboundChangeDifficultyPacket(levelData.getDifficulty(), levelData.isDifficultyLocked()));
         serverGamePacketListenerImpl.send(new ClientboundPlayerAbilitiesPacket(player.getAbilities()));
         serverGamePacketListenerImpl.send(new ClientboundSetHeldSlotPacket(player.getInventory().getSelectedSlot()));
@@ -350,7 +351,7 @@ public abstract class PlayerList {
         // Paper end - Fire PlayerJoinEvent when Player is actually ready
         player.initInventoryMenu();
         // CraftBukkit start
-        org.bukkit.craftbukkit.entity.CraftPlayer bukkitPlayer = player.getBukkitEntity();
+        // org.bukkit.craftbukkit.entity.CraftPlayer bukkitPlayer = player.getBukkitEntity(); // Leaf - move up
 
         // Ensure that player inventory is populated with its viewer
         player.containerMenu.transferTo(player.containerMenu, bukkitPlayer);
@@ -380,10 +381,11 @@ public abstract class PlayerList {
         final List<ServerPlayer> onlinePlayers = Lists.newArrayListWithExpectedSize(this.players.size() - 1); // Paper - Use single player info update packet on join
         for (int i = 0; i < this.players.size(); ++i) {
             ServerPlayer entityplayer1 = (ServerPlayer) this.players.get(i);
+            org.bukkit.craftbukkit.entity.CraftPlayer bukkitPlayer1 = entityplayer1.getBukkitEntity(); // Leaf - only get bukkit entity once
 
-            if (entityplayer1.getBukkitEntity().canSee(bukkitPlayer)) {
+            if (bukkitPlayer1.canSee(bukkitPlayer)) {
                 // Paper start - Add Listing API for Player
-                if (entityplayer1.getBukkitEntity().isListed(bukkitPlayer)) {
+                if (bukkitPlayer1.isListed(bukkitPlayer)) {
                     // Paper end - Add Listing API for Player
                     entityplayer1.connection.send(packet);
                     // Paper start - Add Listing API for Player
@@ -393,7 +395,7 @@ public abstract class PlayerList {
                 // Paper end - Add Listing API for Player
             }
 
-            if (entityplayer1 == player || !bukkitPlayer.canSee(entityplayer1.getBukkitEntity())) { // Paper - Use single player info update packet on join; Don't include joining player
+            if (entityplayer1 == player || !bukkitPlayer.canSee(bukkitPlayer1)) { // Paper - Use single player info update packet on join; Don't include joining player
                 continue;
             }
 
@@ -604,9 +606,10 @@ public abstract class PlayerList {
     }
     public net.kyori.adventure.text.@Nullable Component remove(ServerPlayer player, net.kyori.adventure.text.Component leaveMessage) {
         // Paper end - Fix kick event leave message not being sent
-        org.purpurmc.purpur.task.BossBarTask.removeFromAll(player.getBukkitEntity()); // Purpur - Implement TPSBar
+        org.bukkit.craftbukkit.entity.CraftPlayer bukkitPlayer = player.getBukkitEntity(); // Leaf - only get bukkit entity once
+        org.purpurmc.purpur.task.BossBarTask.removeFromAll(bukkitPlayer); // Purpur - Implement TPSBar
         org.leavesmc.leaves.protocol.core.LeavesProtocolManager.handlePlayerLeave(player); // Leaves - protocol
-        net.minecraft.server.network.ServerGamePacketListenerImpl.afkCooldown.remove(player.getBukkitEntity().getUniqueId()); // Leaf - Improve Purpur AFK system
+        net.minecraft.server.network.ServerGamePacketListenerImpl.afkCooldown.remove(bukkitPlayer.getUniqueId()); // Leaf - Improve Purpur AFK system
         ServerLevel serverLevel = player.level();
         player.awardStat(Stats.LEAVE_GAME);
         // CraftBukkit start - Quitting must be before we do final save of data, in case plugins need to modify it
@@ -615,9 +618,9 @@ public abstract class PlayerList {
             player.closeContainer(org.bukkit.event.inventory.InventoryCloseEvent.Reason.DISCONNECT); // Paper - Inventory close reason
         }
 
-        org.bukkit.event.player.PlayerQuitEvent playerQuitEvent = new org.bukkit.event.player.PlayerQuitEvent(player.getBukkitEntity(), getQuitMsg(leaveMessage, player.getBukkitEntity()), player.quitReason); // Paper - Adventure & Add API for quit reason // Leaf - Configurable connection message - quit message
+        org.bukkit.event.player.PlayerQuitEvent playerQuitEvent = new org.bukkit.event.player.PlayerQuitEvent(bukkitPlayer, getQuitMsg(leaveMessage, bukkitPlayer), player.quitReason); // Paper - Adventure & Add API for quit reason // Leaf - Configurable connection message - quit message
         this.cserver.getPluginManager().callEvent(playerQuitEvent);
-        player.getBukkitEntity().disconnect();
+        bukkitPlayer.disconnect();
 
         if (this.server.isSameThread()) player.doTick(); // SPIGOT-924 // Paper - Improved watchdog support; don't tick during emergency shutdowns
         // CraftBukkit end
@@ -695,14 +698,14 @@ public abstract class PlayerList {
         for (int i = 0; i < this.players.size(); i++) {
             ServerPlayer otherPlayer = this.players.get(i);
 
-            if (otherPlayer.getBukkitEntity().canSee(player.getBukkitEntity())) {
+            if (otherPlayer.getBukkitEntity().canSee(bukkitPlayer)) {
                 otherPlayer.connection.send(packet);
             } else {
                 otherPlayer.getBukkitEntity().onEntityRemove(player);
             }
         }
         // This removes the scoreboard (and player reference) for the specific player in the manager
-        this.cserver.getScoreboardManager().removePlayer(player.getBukkitEntity());
+        this.cserver.getScoreboardManager().removePlayer(bukkitPlayer);
         // CraftBukkit end
         return playerQuitEvent.quitMessage(); // Paper - Adventure
     }
@@ -1103,6 +1106,7 @@ public abstract class PlayerList {
 
     public void sendPlayerPermissionLevel(ServerPlayer player, LevelBasedPermissionSet permissions, boolean recalculatePermissions) {
         // Paper end - Add sendOpLevel API
+        org.bukkit.craftbukkit.entity.CraftHumanEntity bukkit = player.getBukkitEntity(); // Leaf - only get bukkit entity once
         if (player.connection != null) {
             byte b = switch (permissions.level()) {
                 case ALL -> EntityEvent.PERMISSION_LEVEL_ALL;
@@ -1111,18 +1115,17 @@ public abstract class PlayerList {
                 case ADMINS -> EntityEvent.PERMISSION_LEVEL_ADMINS;
                 case OWNERS -> EntityEvent.PERMISSION_LEVEL_OWNERS;
             };
-            if (b < EntityEvent.PERMISSION_LEVEL_OWNERS && player.getBukkitEntity().hasPermission("purpur.debug.f3n")) b = EntityEvent.PERMISSION_LEVEL_OWNERS; // Purpur - Add permission for F3+N debug
+            if (b < EntityEvent.PERMISSION_LEVEL_OWNERS && bukkit.hasPermission("purpur.debug.f3n")) b = EntityEvent.PERMISSION_LEVEL_OWNERS; // Purpur - Add permission for F3+N debug
             player.connection.send(new ClientboundEntityEventPacket(player, b));
         }
 
         if (recalculatePermissions) { // Paper - Add sendOpLevel API
-        player.getBukkitEntity().recalculatePermissions(); // CraftBukkit
+            bukkit.recalculatePermissions(); // CraftBukkit
         this.server.getCommands().sendCommands(player);
         } // Paper - Add sendOpLevel API
 
         // Purpur start - Barrels and enderchests 6 rows
         if (org.purpurmc.purpur.PurpurConfig.enderChestSixRows && org.purpurmc.purpur.PurpurConfig.enderChestPermissionRows) {
-            org.bukkit.craftbukkit.entity.CraftHumanEntity bukkit = player.getBukkitEntity();
             if (bukkit.hasPermission("purpur.enderchest.rows.six")) {
                 player.sixRowEnderchestSlotCount = 54;
             } else if (bukkit.hasPermission("purpur.enderchest.rows.five")) {
@@ -1193,10 +1196,11 @@ public abstract class PlayerList {
     }
 
     public void broadcast(@Nullable Player except, double x, double y, double z, double radius, ResourceKey<Level> dimension, Packet<?> packet) {
+        org.bukkit.entity.Entity bukkitExcept = except == null ? null : except.getBukkitEntity(); // Leaf - only get bukkit entity once
         for (int i = 0; i < this.players.size(); i++) {
             ServerPlayer serverPlayer = this.players.get(i);
             // CraftBukkit start - Test if player receiving packet can see the source of the packet
-            if (except != null && !serverPlayer.getBukkitEntity().canSee(except.getBukkitEntity())) {
+            if (bukkitExcept != null && !serverPlayer.getBukkitEntity().canSee(bukkitExcept)) {
                continue;
             }
             // CraftBukkit end
@@ -1409,13 +1413,14 @@ public abstract class PlayerList {
         for (ServerPlayer serverPlayer : this.players) {
             boolean flag2 = shouldFilterMessageTo.test(serverPlayer);
             // Paper start - don't send player chat packets from vanished players
-            if (sender != null && !serverPlayer.getBukkitEntity().canSee(sender.getBukkitEntity())) {
+            org.bukkit.craftbukkit.entity.CraftPlayer craftPlayer = serverPlayer.getBukkitEntity(); // Leaf - only get bukkit entity once
+            if (sender != null && !craftPlayer.canSee(sender.getBukkitEntity())) {
                 serverPlayer.connection.send(unsignedFunction != null
-                    ? new net.minecraft.network.protocol.game.ClientboundDisguisedChatPacket(unsignedFunction.apply(serverPlayer.getBukkitEntity()), boundChatType)
+                    ? new net.minecraft.network.protocol.game.ClientboundDisguisedChatPacket(unsignedFunction.apply(craftPlayer), boundChatType)
                     : disguised);
                 continue;
             }
-            serverPlayer.sendChatMessage(outgoingChatMessage, flag2, boundChatType, unsignedFunction == null ? null : unsignedFunction.apply(serverPlayer.getBukkitEntity()));
+            serverPlayer.sendChatMessage(outgoingChatMessage, flag2, boundChatType, unsignedFunction == null ? null : unsignedFunction.apply(craftPlayer));
             // Paper end
             flag1 |= flag2 && message.isFullyFiltered();
         }
diff --git a/net/minecraft/world/effect/SaturationMobEffect.java b/net/minecraft/world/effect/SaturationMobEffect.java
index 622c23f4570d07de8bee9623bf900aabb3331ded..efd86376071e7aa5c2f71a24992f97042531f710 100644
--- a/net/minecraft/world/effect/SaturationMobEffect.java
+++ b/net/minecraft/world/effect/SaturationMobEffect.java
@@ -14,13 +14,16 @@ class SaturationMobEffect extends InstantenousMobEffect {
         if (entity instanceof Player player) {
             // CraftBukkit start
             int oldFoodLevel = player.getFoodData().foodLevel;
-            org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(player, amplifier + 1 + oldFoodLevel);
+            // Leaf start - only get bukkit entity once
+            org.bukkit.craftbukkit.entity.CraftPlayer bukkitPlayer = (org.bukkit.craftbukkit.entity.CraftPlayer) player.getBukkitEntity();
+            org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(bukkitPlayer, amplifier + 1 + oldFoodLevel);
             if (!event.isCancelled()) {
                 if (player.level().purpurConfig.playerBurpWhenFull && event.getFoodLevel() == 20 && oldFoodLevel < 20) player.burpDelay = player.level().purpurConfig.playerBurpDelay; // Purpur - Burp delay
                 player.getFoodData().eat(event.getFoodLevel() - oldFoodLevel, entity.level().purpurConfig.humanSaturationRegenAmount); // Purpur - Config MobEffect by world
             }
 
-            ((org.bukkit.craftbukkit.entity.CraftPlayer) player.getBukkitEntity()).sendHealthUpdate();
+            bukkitPlayer.sendHealthUpdate();
+            // Leaf end
             // CraftBukkit end
         }
 
diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index d05e653fa5bb9273b791bd9b21695ca44b9c3c5d..e6d516b224402f1bb5dd8b446f731dc23020ccd0 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -1319,8 +1319,7 @@ public abstract class Entity implements SyncedDataHolder, DebugValueSource, Name
                     }
                 }
                 // CraftBukkit start
-                if (this.horizontalCollision && this.getBukkitEntity() instanceof org.bukkit.entity.Vehicle) {
-                    org.bukkit.entity.Vehicle vehicle = (org.bukkit.entity.Vehicle) this.getBukkitEntity();
+                if (this.horizontalCollision && this.getBukkitEntity() instanceof org.bukkit.entity.Vehicle vehicle) { // Leaf - only get bukkit entity once
                     org.bukkit.block.Block block = this.level.getWorld().getBlockAt(Mth.floor(this.getX()), Mth.floor(this.getY()), Mth.floor(this.getZ()));
 
                     if (movement.x > vec3.x) {
@@ -3431,8 +3430,11 @@ public abstract class Entity implements SyncedDataHolder, DebugValueSource, Name
 
             if (force || this.canRide(entity) && entity.canAddPassenger(this)) {
                 // CraftBukkit start
-                if (entity.getBukkitEntity() instanceof org.bukkit.entity.Vehicle && this.getBukkitEntity() instanceof org.bukkit.entity.LivingEntity) {
-                    org.bukkit.event.vehicle.VehicleEnterEvent event = new org.bukkit.event.vehicle.VehicleEnterEvent((org.bukkit.entity.Vehicle) entity.getBukkitEntity(), this.getBukkitEntity());
+                // Leaf start - only get bukkit entity once
+                org.bukkit.entity.Entity bukkitEntity = this.getBukkitEntity();
+                org.bukkit.entity.Entity mount = entity.getBukkitEntity();
+                if (mount instanceof org.bukkit.entity.Vehicle vehicle && bukkitEntity instanceof org.bukkit.entity.LivingEntity livingEntity) {
+                    org.bukkit.event.vehicle.VehicleEnterEvent event = new org.bukkit.event.vehicle.VehicleEnterEvent(vehicle, livingEntity);
                     // Suppress during worldgen
                     if (this.valid) {
                         org.bukkit.Bukkit.getPluginManager().callEvent(event);
@@ -3442,7 +3444,8 @@ public abstract class Entity implements SyncedDataHolder, DebugValueSource, Name
                     }
                 }
 
-                org.bukkit.event.entity.EntityMountEvent event = new org.bukkit.event.entity.EntityMountEvent(this.getBukkitEntity(), entity.getBukkitEntity());
+                org.bukkit.event.entity.EntityMountEvent event = new org.bukkit.event.entity.EntityMountEvent(bukkitEntity, mount);
+                // Leaf end
                 // Suppress during worldgen
                 if (this.valid) {
                     org.bukkit.Bukkit.getPluginManager().callEvent(event);
@@ -3546,25 +3549,34 @@ public abstract class Entity implements SyncedDataHolder, DebugValueSource, Name
             throw new IllegalStateException("Use x.stopRiding(y), not y.removePassenger(x)");
         } else {
             // CraftBukkit start
-            org.bukkit.craftbukkit.entity.CraftEntity craft = (org.bukkit.craftbukkit.entity.CraftEntity) passenger.getBukkitEntity().getVehicle();
+            // Leaf start - only get bukkit entity once
+            org.bukkit.craftbukkit.entity.CraftEntity bukkitPassenger = passenger.getBukkitEntity();
+            org.bukkit.craftbukkit.entity.CraftEntity bukkitEntity = this.getBukkitEntity();
+            org.bukkit.craftbukkit.entity.CraftEntity craft = (org.bukkit.craftbukkit.entity.CraftEntity) bukkitPassenger.getVehicle();
             Entity orig = craft == null ? null : craft.getHandle();
-            if (this.getBukkitEntity() instanceof org.bukkit.entity.Vehicle && passenger.getBukkitEntity() instanceof org.bukkit.entity.LivingEntity) {
+            if (bukkitEntity instanceof org.bukkit.entity.Vehicle vehicle && bukkitPassenger instanceof org.bukkit.entity.LivingEntity livingPassenger) {
                 org.bukkit.event.vehicle.VehicleExitEvent event = new org.bukkit.event.vehicle.VehicleExitEvent(
-                        (org.bukkit.entity.Vehicle) this.getBukkitEntity(),
-                        (org.bukkit.entity.LivingEntity) passenger.getBukkitEntity(), !suppressCancellation // Paper - Force entity dismount during teleportation
+                    vehicle,
+                    livingPassenger,
+                    !suppressCancellation // Paper - Force entity dismount during teleportation
                 );
                 // Suppress during worldgen
                 if (this.valid) {
                     org.bukkit.Bukkit.getPluginManager().callEvent(event);
                 }
-                org.bukkit.craftbukkit.entity.CraftEntity craftn = (org.bukkit.craftbukkit.entity.CraftEntity) passenger.getBukkitEntity().getVehicle();
+                org.bukkit.craftbukkit.entity.CraftEntity craftn = (org.bukkit.craftbukkit.entity.CraftEntity) bukkitPassenger.getVehicle();
                 Entity n = craftn == null ? null : craftn.getHandle();
                 if (event.isCancelled() || n != orig) {
                     return false;
                 }
             }
 
-            org.bukkit.event.entity.EntityDismountEvent event = new org.bukkit.event.entity.EntityDismountEvent(passenger.getBukkitEntity(), this.getBukkitEntity(), !suppressCancellation); // Paper - Force entity dismount during teleportation
+            org.bukkit.event.entity.EntityDismountEvent event = new org.bukkit.event.entity.EntityDismountEvent(
+                bukkitPassenger,
+                bukkitEntity,
+                !suppressCancellation // Paper - Force entity dismount during teleportation
+            );
+            // Leaf end
             // Suppress during worldgen
             if (this.valid) {
                 org.bukkit.Bukkit.getPluginManager().callEvent(event);
diff --git a/net/minecraft/world/food/FoodData.java b/net/minecraft/world/food/FoodData.java
index 5fa21ff6624df90925a6850f9f9f092396ed8b83..32b1c25f278f9c75a497da7e0d39910a848c8dd7 100644
--- a/net/minecraft/world/food/FoodData.java
+++ b/net/minecraft/world/food/FoodData.java
@@ -37,12 +37,14 @@ public class FoodData {
     // CraftBukkit start
     public void eat(FoodProperties foodProperties, net.minecraft.world.item.ItemStack stack, ServerPlayer serverPlayer) {
         int oldFoodLevel = this.foodLevel;
-        org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(serverPlayer, foodProperties.nutrition() + oldFoodLevel, stack);
+        // Leaf start - only get bukkit entity once
+        org.bukkit.craftbukkit.entity.CraftPlayer bukkitPlayer = serverPlayer.getBukkitEntity();
+        org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(bukkitPlayer, foodProperties.nutrition() + oldFoodLevel, stack);
         if (!event.isCancelled()) {
             if (serverPlayer.level().purpurConfig.playerBurpWhenFull && event.getFoodLevel() == 20 && oldFoodLevel < 20) serverPlayer.burpDelay = serverPlayer.level().purpurConfig.playerBurpDelay; // Purpur - Burp after eating food fills hunger bar completely
             this.add(event.getFoodLevel() - oldFoodLevel, foodProperties.saturation());
         }
-        serverPlayer.getBukkitEntity().sendHealthUpdate();
+        bukkitPlayer.sendHealthUpdate();
     }
     // CraftBukkit end
 
@@ -55,13 +57,16 @@ public class FoodData {
                 this.saturationLevel = Math.max(this.saturationLevel - 1.0F, 0.0F);
             } else if (difficulty != Difficulty.PEACEFUL) {
                 // CraftBukkit start
-                org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(player, Math.max(this.foodLevel - 1, 0));
+                // Leaf start - only get bukkit entity once
+                org.bukkit.craftbukkit.entity.CraftPlayer bukkitPlayer = player.getBukkitEntity();
+                org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(bukkitPlayer, Math.max(this.foodLevel - 1, 0));
 
                 if (!event.isCancelled()) {
                     this.foodLevel = event.getFoodLevel();
                 }
 
-                player.connection.send(new net.minecraft.network.protocol.game.ClientboundSetHealthPacket(player.getBukkitEntity().getScaledHealth(), this.foodLevel, this.saturationLevel));
+                player.connection.send(new net.minecraft.network.protocol.game.ClientboundSetHealthPacket(bukkitPlayer.getScaledHealth(), this.foodLevel, this.saturationLevel));
+                // Leaf end
                 // CraftBukkit end
             }
         }
diff --git a/net/minecraft/world/level/block/CakeBlock.java b/net/minecraft/world/level/block/CakeBlock.java
index 576e00d1e9eb2a692b1ee0f1086ee0db230d4e7e..839bf381159bc95a029a45335dbb524caf63610c 100644
--- a/net/minecraft/world/level/block/CakeBlock.java
+++ b/net/minecraft/world/level/block/CakeBlock.java
@@ -106,14 +106,16 @@ public class CakeBlock extends Block {
             // player.getFoodData().eat(2, 0.1F);
             int oldFoodLevel = player.getFoodData().foodLevel;
 
-            org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(player, 2 + oldFoodLevel);
+            // Leaf start - only get bukkit entity once
+            org.bukkit.craftbukkit.entity.CraftPlayer bukkitPlayer = ((net.minecraft.server.level.ServerPlayer) player).getBukkitEntity();
+            org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(bukkitPlayer, 2 + oldFoodLevel);
 
             if (!event.isCancelled()) {
                 if (player.level().purpurConfig.playerBurpWhenFull && event.getFoodLevel() == 20 && oldFoodLevel < 20) player.burpDelay = player.level().purpurConfig.playerBurpDelay; // Purpur - Burp after eating food fills hunger bar completely
                 player.getFoodData().eat(event.getFoodLevel() - oldFoodLevel, 0.1F);
             }
 
-            ((net.minecraft.server.level.ServerPlayer) player).getBukkitEntity().sendHealthUpdate();
+            bukkitPlayer.sendHealthUpdate();
             // CraftBukkit end
             // Paper - move up
             level.gameEvent(player, GameEvent.EAT, pos);
