From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: hayanesuru <hayanesuru@outlook.jp>
Date: Sun, 25 May 2025 21:49:52 +0900
Subject: [PATCH] fix brain pathfinding


diff --git a/net/minecraft/world/entity/ai/behavior/AcquirePoi.java b/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
index a2d707ec323a2abf07a84a8d347a47d6193588f1..f4da6890e5580e9c7e4c7209b473287ee5f5c9d1 100644
--- a/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
+++ b/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
@@ -168,9 +168,11 @@ public class AcquirePoi {
                 set.add(pair.getSecond());
             }
             // Leaf start
-            var nav = mob.getNavigation();
-            var path = nav.createPath(set, i);
-            if (path != null) nav.brainPath = path;
+            var path = mob.getNavigation().createPath(set, i);
+            var exec = ((ServerLevel) mob.level()).asyncGoalExecutor;
+            if (exec != null && path != null) {
+                exec.submitBrainPath(path);
+            }
             return path;
             // Leaf end
         }
diff --git a/net/minecraft/world/entity/ai/behavior/LongJumpToRandomPos.java b/net/minecraft/world/entity/ai/behavior/LongJumpToRandomPos.java
index ce1c6360077825ab6a00a09563c140b065c95d7a..bf89450ebe7d5cf1b5864b9b42e86fd45a4691aa 100644
--- a/net/minecraft/world/entity/ai/behavior/LongJumpToRandomPos.java
+++ b/net/minecraft/world/entity/ai/behavior/LongJumpToRandomPos.java
@@ -163,9 +163,12 @@ public class LongJumpToRandomPos<E extends Mob> extends Behavior<E> {
                     if (vec31 != null) {
                         entity.getBrain().setMemory(MemoryModuleType.LOOK_TARGET, new BlockPosTracker(jumpTarget));
                         PathNavigation navigation = entity.getNavigation();
+                        Path path = navigation.createPath(jumpTarget, 0, 8);
                         // Leaf start
-                        var path = navigation.createPath(jumpTarget, 0, 8);
-                        if (path != null) navigation.brainPath = path;
+                        var exec = ((ServerLevel) entity.level()).asyncGoalExecutor;
+                        if (exec != null && path != null) {
+                            exec.submitBrainPath(path);
+                        }
                         // Leaf end
                         if (path == null || !path.canReach()) {
                             this.chosenJump = vec31;
diff --git a/net/minecraft/world/entity/ai/behavior/MoveToTargetSink.java b/net/minecraft/world/entity/ai/behavior/MoveToTargetSink.java
index deeebfd6d776935760d035ce37cc1f4ad03832ad..bea962a642f85129f339a53eca1fcc6b29e8423e 100644
--- a/net/minecraft/world/entity/ai/behavior/MoveToTargetSink.java
+++ b/net/minecraft/world/entity/ai/behavior/MoveToTargetSink.java
@@ -140,6 +140,12 @@ public class MoveToTargetSink extends Behavior<Mob> {
                     if (vec3 != null) {
                         // try recalculating the path using a random position
                         this.path = owner.getNavigation().createPath(vec3.x, vec3.y, vec3.z, 0);
+                        // Leaf start
+                        var exec = ((ServerLevel) owner.level()).asyncGoalExecutor;
+                        if (exec != null && path != null) {
+                            exec.submitBrainPath(path);
+                        }
+                        // Leaf end
                         this.finishedProcessing = false;
                         return;
                     }
@@ -188,13 +194,26 @@ public class MoveToTargetSink extends Behavior<Mob> {
         if (this.reachedTarget(entity, walkTarget)) {
             brain.eraseMemory(MemoryModuleType.CANT_REACH_WALK_TARGET_SINCE);
         }
-        return entity.getNavigation().createPath(blockPos, 0);
+        // Leaf start
+        var path1 = entity.getNavigation().createPath(blockPos, 0);
+        var exec = ((ServerLevel) entity.level()).asyncGoalExecutor;
+        if (exec != null && path1 != null) {
+            exec.submitBrainPath(path1);
+        }
+        return path1;
+        // Leaf end
     }
     // Kaiiju end
 
     private boolean tryComputePath(Mob mob, WalkTarget target, long time) {
         BlockPos blockPos = target.getTarget().currentBlockPosition();
         this.path = mob.getNavigation().createPath(blockPos, 0);
+        // Leaf start
+        var exec = ((ServerLevel) mob.level()).asyncGoalExecutor;
+        if (exec != null && this.path != null) {
+            exec.submitBrainPath(this.path);
+        }
+        // Leaf end
         this.speedModifier = target.getSpeedModifier();
         Brain<?> brain = mob.getBrain();
         if (this.reachedTarget(mob, target)) {
@@ -214,6 +233,11 @@ public class MoveToTargetSink extends Behavior<Mob> {
             Vec3 posTowards = DefaultRandomPos.getPosTowards((PathfinderMob)mob, 10, 7, Vec3.atBottomCenterOf(blockPos), (float) (Math.PI / 2));
             if (posTowards != null) {
                 this.path = mob.getNavigation().createPath(posTowards.x, posTowards.y, posTowards.z, 0);
+                // Leaf start
+                if (exec != null && this.path != null) {
+                    exec.submitBrainPath(this.path);
+                }
+                // Leaf end
                 return this.path != null;
             }
         }
diff --git a/net/minecraft/world/entity/ai/behavior/PrepareRamNearestTarget.java b/net/minecraft/world/entity/ai/behavior/PrepareRamNearestTarget.java
index dfd39ff8f4a17b42bda60f2bcec5ebc163c25954..03749b171a716d30044545e5c32fc538a95a68e4 100644
--- a/net/minecraft/world/entity/ai/behavior/PrepareRamNearestTarget.java
+++ b/net/minecraft/world/entity/ai/behavior/PrepareRamNearestTarget.java
@@ -163,6 +163,12 @@ public class PrepareRamNearestTarget<E extends PathfinderMob> extends Behavior<E
             PathNavigation navigation = pathfinder.getNavigation();
             return list.stream().sorted(Comparator.comparingDouble(pathfinder.blockPosition()::distSqr)).filter(pos -> {
                 Path path = navigation.createPath(pos, 0);
+                // Leaf start
+                var exec = ((ServerLevel) pathfinder.level()).asyncGoalExecutor;
+                if (exec != null && path != null) {
+                    exec.submitBrainPath(path);
+                }
+                // Leaf end
                 return path != null && path.canReach();
             }).findFirst();
         }
diff --git a/net/minecraft/world/entity/ai/behavior/VillagerMakeLove.java b/net/minecraft/world/entity/ai/behavior/VillagerMakeLove.java
index c0c5e1234bbb9b960db986d2e4e73abb9c03ab34..5b6bce7f817cffed0e205ea5cc71835c29a69241 100644
--- a/net/minecraft/world/entity/ai/behavior/VillagerMakeLove.java
+++ b/net/minecraft/world/entity/ai/behavior/VillagerMakeLove.java
@@ -102,10 +102,12 @@ public class VillagerMakeLove extends Behavior<Villager> {
     }
 
     private boolean canReach(Villager villager, BlockPos pos, Holder<PoiType> poiType) {
+        Path path = villager.getNavigation().createPath(pos, poiType.value().validRange());
         // Leaf start
-        var navigation = villager.getNavigation();
-        var path = navigation.createPath(pos, poiType.value().validRange());
-        if (path != null) navigation.brainPath = path;
+        var exec = ((ServerLevel) villager.level()).asyncGoalExecutor;
+        if (exec != null && path != null) {
+            exec.submitBrainPath(path);
+        }
         // Leaf end
         return path != null && path.canReach();
     }
diff --git a/net/minecraft/world/entity/ai/behavior/YieldJobSite.java b/net/minecraft/world/entity/ai/behavior/YieldJobSite.java
index 0b8d40a0c030ca364259cdcaa1074544594aa9f9..3951119d0059a7db6a9fe4838c01d66c6a3e2465 100644
--- a/net/minecraft/world/entity/ai/behavior/YieldJobSite.java
+++ b/net/minecraft/world/entity/ai/behavior/YieldJobSite.java
@@ -82,10 +82,12 @@ public class YieldJobSite {
     }
 
     private static boolean canReachPos(PathfinderMob mob, BlockPos pos, PoiType poi) {
+        Path path = mob.getNavigation().createPath(pos, poi.validRange());
         // Leaf start
-        var navigation = mob.getNavigation();
-        var path = navigation.createPath(pos, poi.validRange());
-        if (path != null) navigation.brainPath = path;
+        var exec = ((net.minecraft.server.level.ServerLevel) mob.level()).asyncGoalExecutor;
+        if (exec != null && path != null) {
+            exec.submitBrainPath(path);
+        }
         // Leaf end
         return path != null && path.canReach();
     }
diff --git a/net/minecraft/world/entity/ai/navigation/PathNavigation.java b/net/minecraft/world/entity/ai/navigation/PathNavigation.java
index 3481441d5f5742be3aef6d3514c16e18b9ac3656..b9a5a661687d38381760b4236cf53e2beea2ccd1 100644
--- a/net/minecraft/world/entity/ai/navigation/PathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/PathNavigation.java
@@ -53,7 +53,6 @@ public abstract class PathNavigation {
     public final PathFinder pathFinder;
     private boolean isStuck;
     private float requiredPathLength = 16.0F;
-    public Path brainPath; // Leaf
 
     public PathNavigation(Mob mob, Level level) {
         this.mob = mob;
