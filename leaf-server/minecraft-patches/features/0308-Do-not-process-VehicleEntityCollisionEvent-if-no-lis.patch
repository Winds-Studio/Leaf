From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Github Actions <artyom030803@gmail.com>
Date: Tue, 31 Mar 2026 09:30:12 +0300
Subject: [PATCH] Do not process VehicleEntityCollisionEvent if no listeners


diff --git a/net/minecraft/world/entity/vehicle/VehicleEntity.java b/net/minecraft/world/entity/vehicle/VehicleEntity.java
index f7108e9346db56882c746c869a7d62118b430b20..f75b2cf00507377e0786658e63b92503c5f83635 100644
--- a/net/minecraft/world/entity/vehicle/VehicleEntity.java
+++ b/net/minecraft/world/entity/vehicle/VehicleEntity.java
@@ -21,9 +21,11 @@ public abstract class VehicleEntity extends Entity {
     protected static final EntityDataAccessor<Integer> DATA_ID_HURT = SynchedEntityData.defineId(VehicleEntity.class, EntityDataSerializers.INT);
     protected static final EntityDataAccessor<Integer> DATA_ID_HURTDIR = SynchedEntityData.defineId(VehicleEntity.class, EntityDataSerializers.INT);
     protected static final EntityDataAccessor<Float> DATA_ID_DAMAGE = SynchedEntityData.defineId(VehicleEntity.class, EntityDataSerializers.FLOAT);
+    public final boolean hasVehicleEntityCollisionEvent; // Leaf - Do not process VehicleEntityCollisionEvent if no listeners
 
     public VehicleEntity(EntityType<?> type, Level level) {
         super(type, level);
+        this.hasVehicleEntityCollisionEvent = org.bukkit.event.vehicle.VehicleEntityCollisionEvent.getHandlerList().getRegisteredListeners().length > 0; // Leaf - Do not process VehicleEntityCollisionEvent if no listeners
     }
 
     @Override
diff --git a/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java b/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
index 33fb59123ce59b8fd5c9f84bc31e52d21ad060ff..d583d3e4d793cde47bf1ca029b5848597d080801 100644
--- a/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
+++ b/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
@@ -189,11 +189,15 @@ public abstract class AbstractBoat extends VehicleEntity implements Leashable {
             if (entity.getBoundingBox().minY < this.getBoundingBox().maxY) {
                 // CraftBukkit start
                 if (!this.isPassengerOfSameVehicle(entity)) {
-                    org.bukkit.event.vehicle.VehicleEntityCollisionEvent event = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
-                        (org.bukkit.entity.Vehicle) this.getBukkitEntity(),
-                        entity.getBukkitEntity()
-                    );
-                    if (!event.callEvent()) return;
+                    // Leaf start - Do not process VehicleEntityCollisionEvent if no listeners
+                    if (this.hasVehicleEntityCollisionEvent) {
+                        org.bukkit.event.vehicle.VehicleEntityCollisionEvent event = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
+                            (org.bukkit.entity.Vehicle) this.getBukkitEntity(),
+                            entity.getBukkitEntity()
+                        );
+                        if (!event.callEvent()) return;
+                    }
+                    // Leaf end
                 }
                 // CraftBukkit end
                 super.push(entity);
@@ -201,11 +205,15 @@ public abstract class AbstractBoat extends VehicleEntity implements Leashable {
         } else if (entity.getBoundingBox().minY <= this.getBoundingBox().minY) {
             // CraftBukkit start
             if (!this.isPassengerOfSameVehicle(entity)) {
-                org.bukkit.event.vehicle.VehicleEntityCollisionEvent event = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
-                    (org.bukkit.entity.Vehicle) this.getBukkitEntity(),
-                    entity.getBukkitEntity()
-                );
-                if (!event.callEvent()) return;
+                // Leaf start - Do not process VehicleEntityCollisionEvent if no listeners
+                if (this.hasVehicleEntityCollisionEvent) {
+                    org.bukkit.event.vehicle.VehicleEntityCollisionEvent event = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
+                        (org.bukkit.entity.Vehicle) this.getBukkitEntity(),
+                        entity.getBukkitEntity()
+                    );
+                    if (!event.callEvent()) return;
+                }
+                // Leaf end
             }
             // CraftBukkit end
             super.push(entity);
diff --git a/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java b/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
index 48099ae66045b6e78ec52832e3b972ac9c9bc246..0144c58164d381ccc64ee2abaa76749b349ad7d6 100644
--- a/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
+++ b/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
@@ -180,9 +180,15 @@ public abstract class AbstractMinecart extends VehicleEntity {
             return false;
         }
 
-        org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent((org.bukkit.entity.Vehicle) this.getBukkitEntity(), entity.getBukkitEntity());
-        return collisionEvent.callEvent();
-        // Paper end - fix VehicleEntityCollisionEvent not called when colliding with player
+        // Leaf start - Do not process VehicleEntityCollisionEvent if no listeners
+        if (hasVehicleEntityCollisionEvent) {
+            org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent((org.bukkit.entity.Vehicle) this.getBukkitEntity(), entity.getBukkitEntity());
+            return collisionEvent.callEvent();
+            // Paper end - fix VehicleEntityCollisionEvent not called when colliding with player
+        } else {
+            return true;
+        }
+        // Leaf end
     }
 
     @Override
diff --git a/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java b/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java
index 4b336bb2690244b5c99bb7dd566e4fd84e031e79..0ba4988e6753bc69da2f1d1004b79859f356877f 100644
--- a/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java
+++ b/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java
@@ -532,13 +532,16 @@ public class NewMinecartBehavior extends MinecartBehavior {
                         && !(entity instanceof AbstractMinecart)
                         && !this.minecart.isVehicle()
                         && !entity.isPassenger()) {
-                        // CraftBukkit start
-                        org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
-                            (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(),
-                            entity.getBukkitEntity()
-                        );
-                        if (!collisionEvent.callEvent()) continue;
-                        // CraftBukkit end
+                        // Leaf start - Do not process VehicleEntityCollisionEvent if no listeners
+                        if (this.minecart.hasVehicleEntityCollisionEvent) {
+                            // CraftBukkit start
+                            org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
+                                (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity.getBukkitEntity()
+                            );
+                            if (!collisionEvent.callEvent()) continue;
+                            // CraftBukkit end
+                        }
+                        // Leaf end
                         boolean flag = entity.startRiding(this.minecart);
                         if (flag) {
                             return true;
@@ -564,15 +567,16 @@ public class NewMinecartBehavior extends MinecartBehavior {
                         || entity.isPassenger()) {
                         // CraftBukkit start
                         if (!this.minecart.isPassengerOfSameVehicle(entity)) {
-                            org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
-                                (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(),
-                                entity.getBukkitEntity()
-                            );
-                            if (!collisionEvent.callEvent()) {
-                                continue;
+                            // Leaf start - Do not process VehicleEntityCollisionEvent if no listeners
+                            if (this.minecart.hasVehicleEntityCollisionEvent) {
+                                org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
+                                    (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity.getBukkitEntity()
+                                );
+                                if (!collisionEvent.callEvent()) continue;
+                                // CraftBukkit end
                             }
+                            // Leaf end
                         }
-                        // CraftBukkit end
                         entity.push(this.minecart);
                         flag = true;
                     }
@@ -581,15 +585,16 @@ public class NewMinecartBehavior extends MinecartBehavior {
         } else {
             for (Entity entity1 : this.level().getEntities(this.minecart, box)) {
                 if (!this.minecart.hasPassenger(entity1) && entity1.isPushable() && entity1 instanceof AbstractMinecart) {
-                    // CraftBukkit start
-                    org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
-                        (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(),
-                        entity1.getBukkitEntity()
-                    );
-                    if (!collisionEvent.callEvent()) {
-                        continue;
+                    // Leaf start - Do not process VehicleEntityCollisionEvent if no listeners
+                    if (this.minecart.hasVehicleEntityCollisionEvent) {
+                        // CraftBukkit start
+                        org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
+                            (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity1.getBukkitEntity()
+                        );
+                        if (!collisionEvent.callEvent()) continue;
+                        // CraftBukkit end
                     }
-                    // CraftBukkit end
+                    // Leaf end
                     entity1.push(this.minecart);
                     flag = true;
                 }
diff --git a/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java b/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
index 7ab2542ddd7494585f240657906ccee80cd7944e..a50fb0543a8e2b4f3be5952134937039230db86d 100644
--- a/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
+++ b/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
@@ -376,20 +376,28 @@ public class OldMinecartBehavior extends MinecartBehavior {
                         && !(entity instanceof AbstractMinecart)
                         && !this.minecart.isVehicle()
                         && !entity.isPassenger()) {
-                        // CraftBukkit start
-                        org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
-                            (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity.getBukkitEntity()
-                        );
-                        if (!collisionEvent.callEvent()) continue;
-                        // CraftBukkit end
-                        entity.startRiding(this.minecart);
-                    } else {
-                        // CraftBukkit start
-                        if (!this.minecart.isPassengerOfSameVehicle(entity)) {
+                        // Leaf start - Do not process VehicleEntityCollisionEvent if no listeners
+                        if (this.minecart.hasVehicleEntityCollisionEvent) {
+                            // CraftBukkit start
                             org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                                 (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity.getBukkitEntity()
                             );
                             if (!collisionEvent.callEvent()) continue;
+                            // CraftBukkit end
+                        }
+                        // Leaf end
+                        entity.startRiding(this.minecart);
+                    } else {
+                        // CraftBukkit start
+                        if (!this.minecart.isPassengerOfSameVehicle(entity)) {
+                            // Leaf start - Do not process VehicleEntityCollisionEvent if no listeners
+                            if (this.minecart.hasVehicleEntityCollisionEvent) {
+                                org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
+                                    (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity.getBukkitEntity()
+                                );
+                                if (!collisionEvent.callEvent()) continue;
+                            }
+                            // Leaf end
                         }
                         // CraftBukkit end
                         entity.push(this.minecart);
@@ -400,10 +408,14 @@ public class OldMinecartBehavior extends MinecartBehavior {
             for (Entity entity1 : this.level().getEntities(this.minecart, aabb)) {
                 if (!this.minecart.hasPassenger(entity1) && entity1.isPushable() && entity1 instanceof AbstractMinecart) {
                     // CraftBukkit start
-                    org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
-                        (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity1.getBukkitEntity()
-                    );
-                    if (!collisionEvent.callEvent()) continue;
+                    // Leaf start - Do not process VehicleEntityCollisionEvent if no listeners
+                    if (this.minecart.hasVehicleEntityCollisionEvent) {
+                        org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
+                            (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity1.getBukkitEntity()
+                        );
+                        if (!collisionEvent.callEvent()) continue;
+                    }
+                    // Leaf end
                     // CraftBukkit end
                     entity1.push(this.minecart);
                 }
