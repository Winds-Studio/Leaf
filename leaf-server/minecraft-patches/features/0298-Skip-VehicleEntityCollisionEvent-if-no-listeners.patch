From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: OverwriteMC <artyom030803@gmail.com>
Date: Tue, 31 Mar 2026 09:30:12 +0300
Subject: [PATCH] Skip VehicleEntityCollisionEvent if no listeners


diff --git a/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java b/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
index 37386231ba0de8cec1a671b95ca9cc96446115be..008c12999ecf36d4884eee7a89a80060cc2e6fb5 100644
--- a/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
+++ b/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
@@ -188,7 +188,7 @@ public abstract class AbstractBoat extends VehicleEntity implements Leashable {
         if (entity instanceof AbstractBoat) {
             if (entity.getBoundingBox().minY < this.getBoundingBox().maxY) {
                 // CraftBukkit start
-                if (!this.isPassengerOfSameVehicle(entity)) {
+                if (!this.isPassengerOfSameVehicle(entity) && org.bukkit.event.vehicle.VehicleEntityCollisionEvent.getHandlerList().getRegisteredListeners().length > 0) { // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                     org.bukkit.event.vehicle.VehicleEntityCollisionEvent event = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                         (org.bukkit.entity.Vehicle) this.getBukkitEntity(),
                         entity.getBukkitEntity()
@@ -200,7 +200,7 @@ public abstract class AbstractBoat extends VehicleEntity implements Leashable {
             }
         } else if (entity.getBoundingBox().minY <= this.getBoundingBox().minY) {
             // CraftBukkit start
-            if (!this.isPassengerOfSameVehicle(entity)) {
+            if (!this.isPassengerOfSameVehicle(entity) && org.bukkit.event.vehicle.VehicleEntityCollisionEvent.getHandlerList().getRegisteredListeners().length > 0) { // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                 org.bukkit.event.vehicle.VehicleEntityCollisionEvent event = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                     (org.bukkit.entity.Vehicle) this.getBukkitEntity(),
                     entity.getBukkitEntity()
diff --git a/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java b/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
index 593734ac811ee10875bcd45b0c8b459732e49cd7..499d423651118225120c141c723119bd0d0710f1 100644
--- a/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
+++ b/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
@@ -187,6 +187,7 @@ public abstract class AbstractMinecart extends VehicleEntity {
             return false;
         }
 
+        if (org.bukkit.event.vehicle.VehicleEntityCollisionEvent.getHandlerList().getRegisteredListeners().length == 0) return true; // Leaf - Skip VehicleEntityCollisionEvent if no listeners
         org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent((org.bukkit.entity.Vehicle) this.getBukkitEntity(), entity.getBukkitEntity());
         return collisionEvent.callEvent();
         // Paper end - fix VehicleEntityCollisionEvent not called when colliding with player
@@ -619,11 +620,13 @@ public abstract class AbstractMinecart extends VehicleEntity {
                 if (!this.level().paperConfig().collisions.allowVehicleCollisions && this.level().paperConfig().collisions.onlyPlayersCollide && !(entity instanceof Player)) return; // Paper - Collision option for requiring a player participant
                 if (!this.hasPassenger(entity)) {
                     // CraftBukkit start
+                    if (org.bukkit.event.vehicle.VehicleEntityCollisionEvent.getHandlerList().getRegisteredListeners().length > 0) { // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                     org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                         (org.bukkit.entity.Vehicle) this.getBukkitEntity(),
                         entity.getBukkitEntity()
                     );
                     if (!collisionEvent.callEvent()) return;
+                    } // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                     // CraftBukkit end
                     double xa = entity.getX() - this.getX();
                     double za = entity.getZ() - this.getZ();
diff --git a/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java b/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java
index 3e0dccee83cb61c300f4dd2e3ff8d463338a3c5e..6a101aa23dcfe69858dfe14a044545063f931ade 100644
--- a/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java
+++ b/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java
@@ -529,6 +529,7 @@ public class NewMinecartBehavior extends MinecartBehavior {
         if (this.minecart.isRideable() && !this.minecart.isVehicle()) {
             List<Entity> entities = this.level().getEntities(this.minecart, hitbox, EntitySelector.pushableBy(this.minecart));
             if (!entities.isEmpty()) {
+                final boolean hasVehicleEntityCollisionEvent = org.bukkit.event.vehicle.VehicleEntityCollisionEvent.getHandlerList().getRegisteredListeners().length > 0; // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                 for (Entity entity : entities) {
                     if (!(entity instanceof Player)
                         && !(entity instanceof IronGolem)
@@ -536,11 +537,13 @@ public class NewMinecartBehavior extends MinecartBehavior {
                         && !this.minecart.isVehicle()
                         && !entity.isPassenger()) {
                         // CraftBukkit start
+                        if (hasVehicleEntityCollisionEvent) { // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                         org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                             (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(),
                             entity.getBukkitEntity()
                         );
                         if (!collisionEvent.callEvent()) continue;
+                        } // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                         // CraftBukkit end
                         boolean pickedUp = entity.startRiding(this.minecart);
                         if (pickedUp) {
@@ -556,6 +559,7 @@ public class NewMinecartBehavior extends MinecartBehavior {
 
     public boolean pushEntities(final AABB hitbox) {
         boolean pushed = false;
+        final boolean hasVehicleEntityCollisionEvent = org.bukkit.event.vehicle.VehicleEntityCollisionEvent.getHandlerList().getRegisteredListeners().length > 0; // Leaf - Skip VehicleEntityCollisionEvent if no listeners
         if (this.minecart.isRideable()) {
             List<Entity> entities = this.level().getEntities(this.minecart, hitbox, EntitySelector.pushableBy(this.minecart));
             if (!entities.isEmpty()) {
@@ -566,7 +570,7 @@ public class NewMinecartBehavior extends MinecartBehavior {
                         || this.minecart.isVehicle()
                         || entity.isPassenger()) {
                         // CraftBukkit start
-                        if (!this.minecart.isPassengerOfSameVehicle(entity)) {
+                        if (!this.minecart.isPassengerOfSameVehicle(entity) && hasVehicleEntityCollisionEvent) { // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                             org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                                 (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(),
                                 entity.getBukkitEntity()
@@ -585,6 +589,7 @@ public class NewMinecartBehavior extends MinecartBehavior {
             for (Entity entity : this.level().getEntities(this.minecart, hitbox)) {
                 if (!this.minecart.hasPassenger(entity) && entity.isPushable() && entity instanceof AbstractMinecart) {
                     // CraftBukkit start
+                    if (hasVehicleEntityCollisionEvent) { // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                     org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                         (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(),
                         entity.getBukkitEntity()
@@ -592,6 +597,7 @@ public class NewMinecartBehavior extends MinecartBehavior {
                     if (!collisionEvent.callEvent()) {
                         continue;
                     }
+                    } // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                     // CraftBukkit end
                     entity.push(this.minecart);
                     pushed = true;
diff --git a/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java b/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
index f5e8b16825c617565768afd6b3e51d5cf0f36c50..799afb86019be0cc7ab502ad7b6ec457aa55688d 100644
--- a/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
+++ b/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
@@ -366,6 +366,7 @@ public class OldMinecartBehavior extends MinecartBehavior {
     @Override
     public boolean pushAndPickupEntities() {
         AABB hitbox = this.minecart.getBoundingBox().inflate(0.2F, 0.0, 0.2F);
+        final boolean hasVehicleEntityCollisionEvent = org.bukkit.event.vehicle.VehicleEntityCollisionEvent.getHandlerList().getRegisteredListeners().length > 0; // Leaf - Skip VehicleEntityCollisionEvent if no listeners
         if (this.minecart.isRideable() && this.getDeltaMovement().horizontalDistanceSqr() >= 0.01) {
             List<Entity> entities = this.level().getEntities(this.minecart, hitbox, EntitySelector.pushableBy(this.minecart));
             if (!entities.isEmpty()) {
@@ -376,15 +377,17 @@ public class OldMinecartBehavior extends MinecartBehavior {
                         && !this.minecart.isVehicle()
                         && !entity.isPassenger()) {
                         // CraftBukkit start
+                        if (hasVehicleEntityCollisionEvent) { // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                         org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                             (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity.getBukkitEntity()
                         );
                         if (!collisionEvent.callEvent()) continue;
+                        } // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                         // CraftBukkit end
                         entity.startRiding(this.minecart);
                     } else {
                         // CraftBukkit start
-                        if (!this.minecart.isPassengerOfSameVehicle(entity)) {
+                        if (!this.minecart.isPassengerOfSameVehicle(entity) && hasVehicleEntityCollisionEvent) { // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                             org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                                 (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity.getBukkitEntity()
                             );
@@ -399,10 +402,12 @@ public class OldMinecartBehavior extends MinecartBehavior {
             for (Entity entity : this.level().getEntities(this.minecart, hitbox)) {
                 if (!this.minecart.hasPassenger(entity) && entity.isPushable() && entity instanceof AbstractMinecart) {
                     // CraftBukkit start
+                    if (hasVehicleEntityCollisionEvent) { // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                     org.bukkit.event.vehicle.VehicleEntityCollisionEvent collisionEvent = new org.bukkit.event.vehicle.VehicleEntityCollisionEvent(
                         (org.bukkit.entity.Vehicle) this.minecart.getBukkitEntity(), entity.getBukkitEntity()
                     );
                     if (!collisionEvent.callEvent()) continue;
+                    } // Leaf - Skip VehicleEntityCollisionEvent if no listeners
                     // CraftBukkit end
                     entity.push(this.minecart);
                 }
