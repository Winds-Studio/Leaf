From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Martijn Muijsers <martijnmuijsers@live.nl>
Date: Tue, 29 Nov 2022 00:45:45 +0100
Subject: [PATCH] Reduce RandomSource instances

License: GPL-3.0-only (https://www.gnu.org/licenses/gpl-3.0.html)
Gale - https://galemc.org

This patch is based on the following patch:
"don't create new random instance"
By: foss-mc <69294560+foss-mc@users.noreply.github.com>
As part of: Patina (https://github.com/PatinaMC/Patina)
Licensed under: GPL-3.0-only (https://www.gnu.org/licenses/gpl-3.0.html)

diff --git a/net/minecraft/server/commands/SpreadPlayersCommand.java b/net/minecraft/server/commands/SpreadPlayersCommand.java
index e850a5938605771271e91e64ea2680b75099336b..e0c0f63adea2bceb6f52bea7e6948f1a217e0f2d 100644
--- a/net/minecraft/server/commands/SpreadPlayersCommand.java
+++ b/net/minecraft/server/commands/SpreadPlayersCommand.java
@@ -114,7 +114,7 @@ public class SpreadPlayersCommand {
             throw ERROR_INVALID_MAX_HEIGHT.create(maxHeight, minY);
         }
 
-        RandomSource random = RandomSource.createThreadLocalInstance();
+        RandomSource random = source.getLevel().getRandom(); // Gale - Patina - reduce RandomSource instances
         double minX = center.x - maxRange;
         double minZ = center.y - maxRange;
         double maxX = center.x + maxRange;
diff --git a/net/minecraft/server/rcon/thread/QueryThreadGs4.java b/net/minecraft/server/rcon/thread/QueryThreadGs4.java
index 9c29b275236ad8a79beb130199113dcbda948a14..f036c4eaf68e957333bdb4b81be0010cbfd98c2e 100644
--- a/net/minecraft/server/rcon/thread/QueryThreadGs4.java
+++ b/net/minecraft/server/rcon/thread/QueryThreadGs4.java
@@ -341,7 +341,7 @@ public class QueryThreadGs4 extends GenericThread {
             this.identBytes[2] = buf[5];
             this.identBytes[3] = buf[6];
             this.ident = new String(this.identBytes, StandardCharsets.UTF_8);
-            this.challenge = RandomSource.createThreadLocalInstance().nextInt(16777216);
+            this.challenge = java.util.concurrent.ThreadLocalRandom.current().nextInt(16777216); // Gale - Patina - reduce RandomSource instances
             this.challengeBytes = String.format(Locale.ROOT, "\t%s%d\u0000", this.ident, this.challenge).getBytes(StandardCharsets.UTF_8);
         }
 
diff --git a/net/minecraft/world/entity/projectile/FishingHook.java b/net/minecraft/world/entity/projectile/FishingHook.java
index 3e29bd82521b18a493f9b5b5b23cdb9c03cc94ba..5a41e4053f71eab4042c672d427f51059587470e 100644
--- a/net/minecraft/world/entity/projectile/FishingHook.java
+++ b/net/minecraft/world/entity/projectile/FishingHook.java
@@ -51,7 +51,7 @@ import org.slf4j.Logger;
 
 public class FishingHook extends Projectile {
     private static final Logger LOGGER = LogUtils.getLogger();
-    private final RandomSource syncronizedRandom = RandomSource.create();
+    private final RandomSource syncronizedRandom; // Gale - Patina - reduce RandomSource instances
     private boolean biting;
     public int outOfWaterTime;
     private static final int MAX_OUT_OF_WATER_TIME = 10;
@@ -89,6 +89,7 @@ public class FishingHook extends Projectile {
         this.minWaitTime = level.paperConfig().fishingTimeRange.minimum;
         this.maxWaitTime = level.paperConfig().fishingTimeRange.maximum;
         // Paper end - Configurable fishing time ranges
+        this.syncronizedRandom = level.getRandom(); // Gale - Patina - reduce RandomSource instances
     }
 
     public FishingHook(final EntityType<? extends FishingHook> type, final Level level) {
diff --git a/net/minecraft/world/entity/raid/Raid.java b/net/minecraft/world/entity/raid/Raid.java
index 0e3475d088a33a7344b88ace8a47e8b900d96185..4c99dc16e2ef711bf1f2b02e04237f65a9f11bac 100644
--- a/net/minecraft/world/entity/raid/Raid.java
+++ b/net/minecraft/world/entity/raid/Raid.java
@@ -123,9 +123,9 @@ public class Raid {
     private int groupsSpawned;
     private int postRaidTicks;
     private int raidCooldownTicks;
-    private final RandomSource random = RandomSource.create();
+    //private final RandomSource random = RandomSource.create(); // Gale - Patina - reduce RandomSource instances
     public final ServerBossEvent raidEvent = new ServerBossEvent(
-        Mth.createInsecureUUID(this.random), RAID_NAME_COMPONENT, BossEvent.BossBarColor.RED, BossEvent.BossBarOverlay.NOTCHED_10
+        Mth.createInsecureUUID(RandomSource.create()), RAID_NAME_COMPONENT, BossEvent.BossBarColor.RED, BossEvent.BossBarOverlay.NOTCHED_10 // Gale - Patina - reduce RandomSource instances
     );
     public int numGroups;
     private Raid.RaidStatus status;
@@ -536,7 +536,7 @@ public class Raid {
         float distAway = 13.0F;
         int range = 64;
         Collection<ServerPlayer> playersInRaid = this.raidEvent.getPlayers();
-        long seed = this.random.nextLong();
+        long seed = level.getRandom().nextLong(); // Gale - Patina - reduce RandomSource instances
 
         for (ServerPlayer player : level.players()) {
             Vec3 playerLoc = player.position();
@@ -559,7 +559,7 @@ public class Raid {
 
         for (Raid.RaiderType raiderType : Raid.RaiderType.VALUES) {
             int numSpawns = this.getDefaultNumSpawns(raiderType, groupNumber, isBonusGroup)
-                + this.getPotentialBonusSpawns(raiderType, this.random, groupNumber, difficulty, isBonusGroup);
+                + this.getPotentialBonusSpawns(raiderType, level.getRandom(), groupNumber, difficulty, isBonusGroup); // Gale - Patina - reduce RandomSource instances
             int ravagersSpawned = 0;
 
             for (int i = 0; i < numSpawns; i++) {
@@ -705,12 +705,13 @@ public class Raid {
         int secondsRemaining = this.raidCooldownTicks / 20;
         float howFar = 0.22F * secondsRemaining - 0.24F;
         BlockPos.MutableBlockPos spawnPos = new BlockPos.MutableBlockPos();
-        float startAngle = this.random.nextFloat() * (float) (Math.PI * 2);
+        RandomSource random = level.getRandom();
+        float startAngle = random.nextFloat() * (float) (Math.PI * 2); // Gale - Patina - reduce RandomSource instances
 
         for (int i = 0; i < maxTries; i++) {
             float angle = startAngle + (float) Math.PI * i / 8.0F;
-            int spawnX = this.center.getX() + Mth.floor(Mth.cos(angle) * 32.0F * howFar) + this.random.nextInt(3) * Mth.floor(howFar);
-            int spawnZ = this.center.getZ() + Mth.floor(Mth.sin(angle) * 32.0F * howFar) + this.random.nextInt(3) * Mth.floor(howFar);
+            int spawnX = this.center.getX() + Mth.floor(Mth.cos(angle) * 32.0F * howFar) + random.nextInt(3) * Mth.floor(howFar); // Gale - Patina - reduce RandomSource instances
+            int spawnZ = this.center.getZ() + Mth.floor(Mth.sin(angle) * 32.0F * howFar) + random.nextInt(3) * Mth.floor(howFar); // Gale - Patina - reduce RandomSource instances
             int spawnY = level.getHeight(Heightmap.Types.WORLD_SURFACE, spawnX, spawnZ);
             if (Mth.abs(spawnY - this.center.getY()) <= 96) {
                 spawnPos.set(spawnX, spawnY, spawnZ);
diff --git a/net/minecraft/world/level/block/entity/TheEndGatewayBlockEntity.java b/net/minecraft/world/level/block/entity/TheEndGatewayBlockEntity.java
index 70500bb68965f1704ff87c9bcf1f2f28efca17c3..4fe190b02290e05c0d0fd957957aee4fc1112905 100644
--- a/net/minecraft/world/level/block/entity/TheEndGatewayBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/TheEndGatewayBlockEntity.java
@@ -252,7 +252,7 @@ public class TheEndGatewayBlockEntity extends TheEndPortalBlockEntity {
     }
 
     private static void spawnGatewayPortal(final ServerLevel level, final BlockPos portalPos, final EndGatewayConfiguration config) {
-        Feature.END_GATEWAY.place(config, level, level.getChunkSource().getGenerator(), RandomSource.create(), portalPos);
+        Feature.END_GATEWAY.place(config, level, level.getChunkSource().getGenerator(), level.getRandom(), portalPos); // Gale - Patina - reduce RandomSource instances
     }
 
     @Override
diff --git a/net/minecraft/world/level/dimension/end/EnderDragonFight.java b/net/minecraft/world/level/dimension/end/EnderDragonFight.java
index 6cfaf75e92d194fe4ad1d404bc12ddec5ca18b8a..4dcd59e1a370a6d8ef2fb6ccfc367b60979490f5 100644
--- a/net/minecraft/world/level/dimension/end/EnderDragonFight.java
+++ b/net/minecraft/world/level/dimension/end/EnderDragonFight.java
@@ -466,7 +466,7 @@ public class EnderDragonFight extends SavedData {
             .registryAccess()
             .lookup(Registries.CONFIGURED_FEATURE)
             .flatMap(registry -> registry.get(EndFeatures.END_GATEWAY_DELAYED))
-            .ifPresent(endGateway -> endGateway.value().place(this.level, this.level.getChunkSource().getGenerator(), RandomSource.create(), pos));
+            .ifPresent(endGateway -> endGateway.value().place(this.level, this.level.getChunkSource().getGenerator(), this.level.getRandom(), pos)); // Gale - Patina - reduce RandomSource instances
     }
 
     public void spawnExitPortal(final boolean activated) {
